# Midnight Node 1 maintenance line

> [!IMPORTANT]
> `midnight-1` is the temporary EffectStream maintenance line for Midnight
> Node 1.x and Ledger-v8. The default `v-next` branch targets Midnight Node 2.x
> and Ledger-v9 and is not merged into this branch.

## Release-operator breaking change

> [!WARNING]
> **BREAKING FOR RELEASE OPERATORS AND AUTOMATION:** every publisher invocation
> now requires an explicit `--dist-tag`. Stable `0.104.x` accepts only
> `midnight-1`; stable `0.200.x` accepts only `latest`; the retained Node-2
> prerelease path accepts only `next`. Legacy invocations that relied on npm's
> implicit `latest` fail before source, registry, tag, release, or branch
> mutation.

This is deliberately an operator/automation interface break. It is **not** a
package API or runtime API break for EffectStream consumers.

## Compatibility contract

This branch is rooted exactly at
`b267fa2e795fcd1bd8e295d4d9b8eb65814b11ee`, the final Node-1-compatible
commit before the Node-2/Ledger-v9 migration. Releases from this line remain in
the stable `0.104.x` family and preserve the following stack unless a future
approved compatibility decision explicitly changes it:

| Component | Maintained generation |
| --- | --- |
| Midnight node | `1.0.0` |
| Midnight indexer | `v4.3.3` |
| Proof server / Ledger | `ledger-8.1.0` |
| Ledger package | `@midnight-ntwrk/ledger-v8@8.1.0` |
| On-chain runtime | v3 (`3.0.0`) |
| Midnight.js | `4.1.1` |
| Compact runtime / Compact JS | `0.16.0` / `2.5.1` |

Node 2.x, Ledger-v9, on-chain runtime v4, and Midnight.js v5 changes belong to
`v-next` and stable EffectStream `0.200.x`. They must not enter this branch as
part of a backport.

## npm channels and installation

Stable `0.104.x` packages are published from `midnight-1` only with the
explicit npm dist-tag `midnight-1`. They never create, move, or repair
`latest`. Stable `0.200.x` packages are published from `v-next` with explicit
dist-tag `latest` and remain npm's default line.

Install the maintained Node-1 line by channel:

```sh
npm install @effectstream/node-sdk@midnight-1
```

Or pin an exact reviewed maintenance version:

```sh
npm install @effectstream/node-sdk@0.104.1
```

Use the default Node-2 line only when the product's Midnight environment has
migrated:

```sh
npm install @effectstream/node-sdk@latest
```

The first new release planned for this branch is `0.104.2`. It is not
published merely by creating or updating this local branch; publication has a
separate audited authorization gate.

## How changes reach this branch

Every candidate change is reviewed against the Node-1 compatibility contract
and receives one of four dispositions:

- `CHERRY_PICK`: the upstream first-parent patch applies without importing
  Node-2 state, and provenance is preserved with `git cherry-pick -x`.
- `MANUAL_PORT`: only the Node-1-compatible behavior is reimplemented; mixed
  Node-2 manifests and lockfiles are never copied wholesale.
- `EXCLUDED`: the change is unnecessary, breaking, or coupled to Node 2.
- `WATCHLIST`: the change is not yet ready for a maintenance decision.

The branch never merges the Node-2/Ledger-v9 migration. Each accepted change
must keep its source PR/SHA, compatibility rationale, maintenance result,
deviations, and test evidence in the ledger below.

## Backport ledger

| Source PR / SHA | Disposition | Compatibility rationale | Resulting maintenance SHA | Deviations / conflicts | Test evidence |
| --- | --- | --- | --- | --- | --- |
| PR #876 / `2c1903c25e0ba348007b80a08a4380f8d835ae52` | `MANUAL_PORT` | Upgraded only the five generic binary wrappers and regenerated the root lock from the Node-1 tree. | `cea1237b225c3feba51d1a80660a6a337118aafa` | No cherry-pick conflict because the port was manual. The Ledger-v9-era upstream lock was not copied. Bun 1.3.10 Linux amd64 regeneration also normalized 39 stale importer-version metadata pairs to the already-declared `0.104.1`; every resolver delta is caused by the five constraints. A disposable-only override selected reviewed decompressor `10.2.2`, then was removed before the full frozen install. | Docker image `oven/bun@sha256:b86c67b531d87b4db11470d9b2bd0c519b1976eee6fcd71634e73abfa6230d2e`: full frozen install passed; graph is bin-wrapper `13.2.0`, decompressor `10.2.2`, downloader `15.2.0`, with no decompressor `5.0.0`. Download/path/version SHA-256 evidence: Bitcoin 28.1 `c2e73c31a0b371bd0721bc8bb6d199736fa1f3d2bb45edb7c8056eab3cec168a`; Celestia app 6.4.10 `48a17d6e523ec0217ea5d13948df3a44c473ebd4a2df5c4f4d5be3080a3352a3`, node 0.28.4 `3289e47299086ad9b98c2be55f096c3347b72cd32b9382f9036b3624878b84de`; NEAR 2.10.7 `bcb0900febf4ed358dc26842cb57a326da5ea00473f6a043dc6aa1975f722825`; ord 0.23.3 `9cd809fa22d1e6202da138efd14a3884e17b74998a98d8f98e389cab38b84897`; Solana 3.0.14 `80f3bc0e3fa6a3090e69bafbfe00b249eaa655c5874ac83aebb6acdb157140ab`. NEAR passed on its native Linux arm64 wrapper fallback at container-local port 28473 after its amd64 binary could not execute under host QEMU. All resources were removed. |
| PR #882 / `75beaac289a09506d53082358d887c714527110d` | `CHERRY_PICK` | PgLite lifecycle correctness fix is independent of Midnight; all nine modified applicability blobs were byte-identical and all eleven added paths were absent on both sides. | `3872a91fc612302459465980dbdf13cdd3b28054` | Applied merge first-parent patch with `-x -m 1`; no conflicts or deviations. Stable source/maintenance patch ID is `18dc87d5f8817992c04efbeee9406206b968403f`. | Pinned Bun 1.3.10 Docker red tree failed across the old retained-socket/default/forced/repeated-close/process-exit/startup-cleanup/in-flight-query/PgTyped paths. Native Linux arm64 green focused command: 25 pass / 0 fail / 97 expectations, including PgTyped success/failure/timeout and single-file SIGTERM. Complete DB+Node green: 59 pass / 2 skip / 2 pre-existing fail; exact clean B1 baseline: 34 pass / 2 fail with the identical unrelated snapshot-handler export and db-emulator workspace-resolution errors, so the delta is 25 passes + 2 fixture skips + 0 new failures. An amd64 QEMU run printed the expected close markers but exceeded three five-second test bounds; all three passed natively. No host ports were published and all resources were removed. |
| PR #887 / `332503c8f9216143a8c805f2a0acbcfd39e5a21d` | `CHERRY_PICK` | Solana transport-error preservation is independent of Midnight; the modified adapter blob matched the source first parent and the added test was absent on both applicability sides. | `a3f677e6521bdf9c2fc7f31f83aaee166a931c5a` | Applied merge first-parent patch with `-x -m 1`; no conflicts or deviations. Stable source/maintenance patch ID is `5fcf1257cb5506e676be9edbcb6b8c83c1506753`. | Pinned Bun 1.3.10 Docker: classifier 5/0/11 expectations; Solana adapter 20/0/23; full batcher 90/0/173. `ECONNREFUSED` and non-Error `socket hang up` reach infrastructure classification; genuine transaction rejection remains non-infrastructure; mixed success remains successful; empty payload keeps the bare error. Only container-local ephemeral ports were used. |
| Playwright / `7c2c270a590e8c195d725ad1797de3a1f42e1266` | `EXCLUDED` | After the B1 lock regeneration, both the root and projected-NFT graphs still resolve one internally aligned `@playwright/test` / `playwright` / `playwright-core` set at `1.61.0`. The source commit repairs the `1.62.1` mismatch introduced only by the Node-2 migration. | None | No template or browser declaration was changed. The conditional manual-port trigger did not occur. | Exact post-B1 manifest/lock assertions passed for root and `templates/projected-nft-preorder`; each graph resolves the three browser packages at `1.61.0`, and the projected-NFT tree remains unchanged. |
| PR #880 / `0277bd184510c84c08078770b0cf7b191c8784b5` and PR #881 Ledger-v9/template migration | `EXCLUDED` | These changes define the Node-2/Ledger-v9 `0.200.x` line. | None | Never merge or copy Node-2 application state. | Both PR #880 and direct migration child `5130be28...` are not ancestors; targeted baseline forbidden-identifier scan found zero matches. |
| Audited generic dual-line release tooling / routing R5 `49d8c5a4789dd15601b3a676698af7e90e14f337` | `MANUAL_PORT` | Branch-aware release/CI safety is shared across both compatibility lines, while the application source remains Node-1/Ledger-v8. | CI `7dfd2567eb32f40f8b95734ddc2687b8e4677ca6`; release `e7fec008a068bec05b2da90ac72758636c17c3b8`; recovery remediation `fee5caa6d948d5cc9c264b8ed130e811511edf9f` | `158efc09...` applied cleanly. `58a33a78...` conflicted only with the older publisher/workflow and absent guard paths; all nine paths were resolved to the exact audited source blobs before continuation. `49d8c5a4...` then applied cleanly. No application manifest, lock, template, README notice, or runtime path was copied. Final selected blobs: CI test `8ebe565605dc6849e3d1b887c3a2c1c1e56603dd`; CI classifier `e6fa7bc1024a8e651fba461b816fa702ad32be8c`; publisher registry test `8dc124278af31d8c94c125ba97523977f397e206`; publisher test `24d67142aba4f4eb50b1f4a68ad4fc13b3ef005d`; publisher `a6c0024d78a604d2a63b8cbcfebc0079c42da4c8`; recovery test `1dabb483d57d9003ace486a7c2dec6c7390a187d`; guard `22fc9de44075495c2fc334ea47b2d0a08bd5c0e0`; guard test `a524192df762abcce80b13872ba171f33744e201`; release workflow `899e8bbc22634907c7580c0d8b56fc08b274cd99`; recovery workflow `abd7c1c6de244997d25873271a6b43034663a60d`; rehearsal workflow `e4235a5aa64a7258a3691588bd0ebea4a70b7411`. Embedded guard SHA-256 is `f1c6f21dd0bfe03ee5fbf469c099e8bbc203e2adce61d819e3f5f1f1e33d8e20`. `main.yaml` is verified by maintenance behavior instead of required whole-file identity. | Pinned Docker: frozen Node-1 install passed; exact audited routing matrix 102/0/351; guard 5 positives, 16 fail-closed negatives, 2 race rejections; all four workflows actionlint-clean. The recovery-drift fixture required mechanically drifting only the 40 disposable version fields because its routing-era `0.104.1` premise equals the real maintenance versions. All owned containers, ports, credentials, registries, and volumes were removed. |
| PR #876 / `2c1903c25e0ba348007b80a08a4380f8d835ae52` lock-repair clarification | `MANUAL_PORT` | The seven accepted binary importers already declare `@xhmikosr/bin-wrapper@^13.2.0`; the current Node-1 root lock was therefore normalized in place with exact native Bun 1.4.0 instead of adding a root decompressor override or copying the Node-2 lock. | `9e3e38c1e64bd24a4a7b3071030a98ec08bedfa9` | The accepted Bun-1.3 regeneration retained stale temporary `@xhmikosr/decompress@10.2.2` override metadata and omitted one EVM workspace file alias, so Bun 1.4 frozen install rejected it. Exact old/intermediate/final lock SHA-256 values are `83134598eb5734aaecfa9bfe35cf807ad0f5592222e102f30f52b78a345de5c6`, `14e0da51ab8c0100f4ce370bbf0b9e4255bb2ebba090a54426e9069f29708b3d`, and `61af639db5c3704ba9e09e03fa1e0d7a5e33e8064121065574eb682a4d7f7fb4`. The final delta is exactly 2 insertions / 1 deletion: remove only stale override metadata and add only `@e2e/evm-contracts/@effectstream/evm-contracts`, already present in PR #876. Package resolutions, all 199 package manifests, all 40 release-version fields, and the Node-1 graph are unchanged. No root override, deleted/rebuilt lock, or copied Node-2 lock. This supersedes the prior Bun-1.3-only limitation. | Official native macOS ARM64 Bun `1.4.0+34cbb9a40`: two controlled normal install passes reproduced the exact intermediate/final bytes; immediate and post-cherry-pick frozen installs passed. Format v1, all 68 workspaces, 2,319 common package entries, the Midnight/Playwright subsets, and the secure chain `13.2.0 → 15.2.0 → 10.2.2` remained unchanged; zero decompressor v5. |
| PR #896 merge `89155bea9b3527628decdbeaeb7fddf777999904` / feature `d8b52e8d63466f7667f1c52f281be02911b164c4` | `CHERRY_PICK` | Generic test-only readiness ordering prevents PgLite/WASM startup time from consuming the preserved 500 ms post-readiness client handshake budget; it is independent of Midnight generation and changes no production gateway. | `36ce6b7fa4c23ce86bedbd4bab8a3c2938f11c1b` | Applied exactly once with `git cherry-pick -x`; no conflict, manual resolution, or deviation. Stable source/maintenance patch ID is `094e74266b0d09cf7220155d18ee02873f13ca3e`; final test blob is `c9d7eaa30e335a34eb0aa11595c9fd1133a1b593`. Production gateway, regenerated root lock, all package manifests/versions, and the complete Node-1/Ledger-v8 compatibility graph are unchanged by this commit. | Native macOS ARM64 Bun 1.4.0: exact focused readiness trio `3 pass / 20 filtered / 0 fail`; unchanged workflow lifecycle pair `24 pass / 0 fail`; no retry, emulation, Docker, retained candidate process, or Bun listener. P2/P3 evidence contains 24 hashed logs (manifest SHA-256 `a929ea3461956a9a4e35be8dfa1f49a6d2b173bdda6400e1827627d22d624b7e`) and a 35-file ordered manifest at SHA-256 `14b403b213ebcd8e696ee88411a51856f6f87589e47f04defe6cb11f7f955adb`. |
| PR #909 merge `333d733f47c5937205aeb8279b622fe9c601cd83` / features `a1802f0e0d063e383159dfd2cce5991c1bf933ba`, `e594d6f8e023c6317488d3c4adeaeb186f38a762` | `CHERRY_PICK` | Shielded-identity display in the `zswap-da` template only: bech32m formatting of the address and the network segment kept in its truncated form. Both commits touch `templates/zswap-da/src` exclusively, change no manifest, lockfile, or runtime package, and read only the connected wallet's shielded key, so the behavior is independent of the ledger generation. | `a7443534116a7d42983e541b602424779821f973`; `cc29f762691922e2989bda9991ce052f68d98e06` | Applied with `git cherry-pick -x`; no conflict, manual resolution, or deviation. The upstream `0.200.1` template bump `98b2b78be3ceb1c4c5a4a84056951c01fe0c918e`, which sits between these commits on `v-next`, was deliberately not picked, so `templates/zswap-da/package.json` and `templates/zswap-da/bun.lock` stay byte-identical to the branch base. This branch's only prior template commit, `8629dda4` (`src/services/api.ts` endpoint rewriting), is untouched and is not reimplemented by any picked commit. | Verified on the combined maintenance tip `97eac81e2c96cc5c85cec787fa78daa2b79e5185`, which carries this and the two backports below. Native macOS ARM64 Bun `1.3.11-canary.1 (17bd6cb1)` in `templates/zswap-da`: frozen install passed (699 installs across 684 packages, no changes) with the lock byte-identical to the base; `bunx tsc -b` exit 0; `bun test` 141 pass / 0 fail / 248 expect() across 9 files; `bun run build` ✓ 9.47 s including the Compact `prebuild`. Resolved app graph is `@effectstream/midnight-contracts@0.104.0` and `@effectstream/wallets@0.104.0` over `@midnight-ntwrk/ledger-v8` and `onchain-runtime-v3`, with no `ledger-v9` present. |
| PR #910 merge `810799f26a5062926b29f71a3a465598ee5e4799` / features `58d6adb292fec21d4f5d9758027be516fc126839`, `b3027a2243679ec4c400568f47c5f374917a8ac3` | `CHERRY_PICK` | Settles a multi-offer ladder take as one merged transaction, on both the built-in and the Lace signing path. The offer-merge is performed in the template's own `services/offerBatch.ts` over the existing `@effectstream/mip-zswap-offer@0.2.0` blob format; no manifest, lockfile, or runtime package changes, and no ledger-generation-specific API is introduced. Requirement (C) of PR #913 builds on this code, which is why it precedes it here. | `2ec6dfc5a280c322793aa14a2aabfb290fd080e9`; `35b50b5cad600fa3b2853de971e05694a80d1d82` | Applied with `git cherry-pick -x`; no conflict, manual resolution, or deviation. | Same combined-tip run as the row above: frozen install passed with an unchanged lock, `tsc -b` exit 0, `bun test` 141 pass / 0 fail / 248 expect() across 9 files, `bun run build` ✓ 9.47 s, Bun `1.3.11-canary.1`. The merged-take unit suite `src/services/offerBatch.test.ts` is part of that green run. |
| PR #913 (open on `v-next`, pending merge) / head `f42b1ca4424cd8f6db4cc6f207d40d0d37a74a9f`; features `3882920e2d783165cbcddc476f7e6ef0871fddad`, `49a7be9c47309e4a6f866d0c3ac60b412c9c3125`, `e7ce2aa07aad196f43df261cc7318aec1592e987`, `fd54a6268047e75a70256da64aaf58931245067c`, `f42b1ca4424cd8f6db4cc6f207d40d0d37a74a9f` | `CHERRY_PICK` | Wallet-scoped local records (`zswap-da:my-offers` / `zswap-da:my-trades` keyed by `<networkId>::<shieldedAddress>`), an own-offer decision dialog replacing the blank connect modal, and per-offer checkboxes for ladder selection. All five commits are confined to `templates/zswap-da/src`; they change no manifest, lockfile, or runtime package, and touch only browser-local state and UI, so nothing depends on the node or ledger generation. | `f35de9e2493fe9c6a111022f987b52be5d21e094`; `f037d95cf55682d1f3538adb9532303f5cd86acf`; `7a802f5cc1ebb85dbe321141b3d0f6aaf9910dbf`; `90dcc41c883b5a22efca9dd2020595c6225aba84`; `97eac81e2c96cc5c85cec787fa78daa2b79e5185` | Applied with `git cherry-pick -x`; no conflict, manual resolution, or deviation. **The source PR is still open on `v-next`**, so the cited provenance is its five branch commits at head `f42b1ca4...` rather than a merge commit; if it is squashed or amended before merge these source SHAs must be re-checked. Behavior deviation to carry: `f42b1ca4...` is **breaking for existing browsers** — pre-scoping flat `zswap-da:my-offers` / `zswap-da:my-trades` values are read as empty and overwritten by the next write, so anyone testing the deployed preprod build loses their local trade log once. Accepted upstream as an alpha-stage change; no on-chain or package-API break. | Same combined-tip run: frozen install passed with an unchanged lock, `bunx tsc -b` exit 0, `bun test` 141 pass / 0 fail / 248 expect() across 9 files (276 ms), `bun run build` ✓ 9.47 s, Bun `1.3.11-canary.1 (17bd6cb1)`. The backported suites `state/scope.test.ts`, `state/myOffers.test.ts`, `state/myTrades.test.ts`, `services/ownOffers.test.ts` and `services/takeSelection.test.ts` are in that run. Wallet-connected browser verification of the take dialogs is still outstanding and belongs to the deployed preprod build, not to this backport. |
| `zswap-da` template dependency bump `98b2b78be3ceb1c4c5a4a84056951c01fe0c918e` (`@effectstream/*` `0.104.0` -> `0.200.1`) | `EXCLUDED` | Defines the Node-2/Ledger-v9 `0.200.x` line; `@effectstream/wallets@0.200.1` depends on `@midnight-ntwrk/ledger-v9`. | None | Nothing from this commit was copied, not even its `templates/zswap-da` slice. The first version of the `zswap-da` backport branch did carry that slice plus a regenerated lock; that violated this contract and was discarded by rebuilding the branch from the base and re-picking without it. Every template on this branch keeps `@effectstream/*` at `0.104.0`. | `98b2b78b...` is not an ancestor of the maintenance tip. `git diff <base> -- templates/zswap-da/package.json templates/zswap-da/bun.lock` is empty; both blobs still equal the base's (`package.json` `822bd218b1a25032bd40be6c19b87674d74aede4`, `bun.lock` `1099e6f609b70d2b35fceb8dd664de6854c88697`) and the manifest still pins `@effectstream/midnight-contracts` and `@effectstream/wallets` at `0.104.0`. The whole backport diff is 30 files, all under `templates/zswap-da/src/`. The resolved template graph contains `@midnight-ntwrk/ledger-v8` and no `ledger-v9`. |
| PR #915 (open on `v-next`, pending merge) / head `c3e361a65d8e40fdc96e52fff6b9561b24d9cf35`; features `e0ec26c57fb603680a4c7a2c8644509e5e758bda`, `5b2b1d3dc26af14336b58a14d14dc433ce356a13`, `c3e361a65d8e40fdc96e52fff6b9561b24d9cf35` | `CHERRY_PICK` | Reference-price display in the `zswap-da` template only: the Swap screen's reference rate, offset and sponsorship threshold, and the Market header's reference/mid-offset stats, all read from the kernel's `GET /v1/quote` and `GET /v1/prices`. All three commits are confined to `templates/zswap-da/src`; they change no manifest, lockfile or runtime package, and introduce no ledger-generation-specific API. The new `Quote` fields are typed **optional** on purpose, so a node that predates the kernel price service — which is every Node-1 deployment on this line until it is redeployed — omits them and the UI degrades to "no reference known" instead of reading `undefined`. That makes the change safe to ship on `midnight-1` ahead of, or entirely without, the kernel side. | `0fbbda0c3613f2c7b063d853fea1261a500a9df8`; `d5b02a1379f8ace5b4551df27d20e9e856eb7012`; `561e989deb32929d51dbba0d61f80ee029d693a9` | Applied with `git cherry-pick -x`; no conflict, manual resolution or deviation. `src/services/api.ts` auto-merged: this branch's `8629dda4` endpoint-rewriting block in `getMidnightConfig` is preserved untouched, and is the *only* difference between this tip and the `v-next` source tip under `templates/zswap-da/src` (`git diff HEAD c3e361a6 -- templates/zswap-da/src` = 1 file, 32 deletions, exactly that block). Stable source/maintenance patch IDs match one-for-one: `b1ee3131d2da8b2bac79d191132ecdcda237ef78`, `1a43ec1fd9af8ca9011c0528c68e92543303ba6c`, `6fde6a6165d23fd8bcfee20b035979c26398b448`. **The source PR is still open on `v-next`**, so the cited provenance is its three branch commits at head `c3e361a6...` rather than a merge commit; if it is squashed or amended before merge these source SHAs must be re-checked. No dependency bump is carried. | Native macOS ARM64 Bun `1.3.11-canary.1 (17bd6cb1)` in `templates/zswap-da`, on the branch tip: `bun install --frozen-lockfile` passed (699 installs across 684 packages, no changes) with the lock byte-identical to the base; `bunx tsc -b` exit 0; `bun test` **176 pass / 0 fail / 375 expect() across 12 files** against a base baseline of 141 pass / 0 fail / 248 expect() across 9 files measured on `7d8d4e23` — a delta of exactly the 35 new cases in `state/format.test.ts`, `state/reference.test.ts` and `services/api.test.ts`, with no pre-existing test changed; `bun run build` ✓ 9.09 s including the Compact `prebuild`. `templates/zswap-da/package.json` (`822bd218b1a25032bd40be6c19b87674d74aede4`) and `bun.lock` (`1099e6f609b70d2b35fceb8dd664de6854c88697`) are the base blobs unchanged; the resolved graph is `@effectstream/midnight-contracts@0.104.0` and `@effectstream/wallets@0.104.0` over `@midnight-ntwrk/ledger-v8`, with no `ledger-v9` present. Browser verification of the reference lines against a kernel that actually serves the new fields is outstanding on both lines and belongs to the deployed build, not to this backport (the `v-next` walkthrough ran against a fixture stub). |
| PR #915 follow-up (open on `v-next`, pending merge) / `43f0ea36d6b2e61d103932e00f1a8066ec621a34`, `689c326ce05d6c0694738e0f8966ed0a24ca94d8` | `CHERRY_PICK` | Same `zswap-da` template scope as the row above, narrowed to how the reference prices are FETCHED: the kernel's `GET /v1/prices` now requires `?tokens=<color>[,<color>...]` (1-50 lower-case colours) and answers `400 VALIDATION` without it, so the template asks about the selected pair instead of pulling the whole price table (master plan section 3a, question Q-11). The commit is confined to `templates/zswap-da/src`; it changes no manifest, lockfile or runtime package and introduces no ledger-generation-specific API. A `400` is treated exactly like the pre-existing `404` - resolved to null, with the header falling back to "no reference" - so a Node-1 stack whose kernel has neither the route nor the new query keeps behaving as it does today, with one fewer request than before (nothing is fetched until a pair is selected). | `ddc2a8e421e5ebb2bd9a05e762b4fb21660b50a5`; `7bb826dfac2981a8ee6f95cd072d675da015849c` | Applied with `git cherry-pick -x`; no conflict, manual resolution or deviation. `src/services/api.ts` auto-merged again and this branch's `8629dda4` endpoint-rewriting block in `getMidnightConfig` is still preserved untouched: `git diff HEAD 43f0ea36 -- templates/zswap-da/src` remains 1 file / 32 deletions, exactly that block. Stable source/maintenance patch IDs match one-for-one: `0f199c731158a9aa63fd4f1c708aae95c9ebf453` and `cd00d137f957a1cb794b490d5fe9a572ce11aa25`. The second commit is a display-continuity fix to the first, kept as its own commit rather than an amendment so both lines carry the same two patches: the keyed cache initially held its response without the key it answered, so a refresh past the 60 s TTL blinked the Market header through "no reference"; the response is now held with its key and the shown value derived from it. **The source PR is still open on `v-next`**, so the cited provenance is the branch commit rather than a merge commit; if it is squashed or amended before merge this source SHA must be re-checked. No dependency bump is carried. | Native macOS ARM64 Bun `1.3.11-canary.1 (17bd6cb1)` in `templates/zswap-da`, on the branch tip: `bun install --frozen-lockfile` passed (699 installs across 684 packages, no changes) with the lock byte-identical to the base; `bunx tsc -b` exit 0; `bun test` **189 pass / 0 fail / 411 expect() across 13 files** against the previous maintenance tip `a264c749`'s 176 pass / 375 expect() across 12 files - a delta of exactly the 13 new cases (5 in `services/api.test.ts` covering the built query, lower-casing and de-duplication, the client-side refusal of an empty or over-50 list with zero requests, and the `400` reason; 8 in the new `state/usePrices.test.ts` covering the sorted-colour-set cache key, one request per key, a shared in-flight request, key isolation, retry after failure and invalidation; 1 in `state/reference.test.ts` for the case-insensitive colour lookup), with no pre-existing test changed; `bun run build` OK 9.47 s including the Compact `prebuild`. Re-run on the tip carrying both picks (`7bb826df`): frozen install unchanged, `bunx tsc -b` exit 0, `bun test` 189 pass / 0 fail / 411 expect() across 13 files, `bun run build` OK 9.15 s. `templates/zswap-da/package.json` (`822bd218b1a25032bd40be6c19b87674d74aede4`) and `bun.lock` (`1099e6f609b70d2b35fceb8dd664de6854c88697`) are still the base blobs unchanged, with zero `ledger-v9` matches in the lock. No Docker stack, no published ports. Browser verification against a kernel that actually serves the filtered route remains outstanding on both lines, as recorded in the row above. |
| `v-next` branch `00043-rate-limit-shared-bucket-runtime` commit `a8fa298b9a8714831706eeae09d24cb04f9fe03e` (PR pending) | `CHERRY_PICK` | Runtime HTTP server only: Fastify is now created with `trustProxy` from the new `EFFECTSTREAM_TRUST_PROXY` env (`true` by default, `false`, or a proxy IP/CIDR list), so `request.ip` is the client behind nginx/Caddy instead of the proxy and per-IP logic such as the offer-files kernel's rate limiter stops sharing one bucket across every user. Touches `packages/effectstream-sdk/utils/src/config.ts` and `packages/node-sdk/runtime/src/api/http-server.ts` plus a new unit test; no manifest, lockfile, ledger, or wallet dependency is involved, so it is independent of the node generation. | `dd968e08ff96718a65a76334749ca1bd703a5d38` | Applied with `git cherry-pick -x`; no conflict, manual resolution, or deviation. **The source is a branch commit, not yet merged on `v-next`**; re-check the SHA if it is squashed or amended before merge. Behavior change to carry: the default is to trust every hop, so a runtime exposed directly without a proxy must set `EFFECTSTREAM_TRUST_PROXY=false` if it relies on `request.ip` being unspoofable. | Native macOS ARM64 Bun `1.3.11-canary.1` on the branch tip: `bun install --frozen-lockfile` passed with the lock unchanged; `bun test packages/node-sdk/runtime/src/api/trust-proxy.test.ts` 4 pass / 0 fail / 9 expect(); root `bunx tsc --noEmit -p tsconfig.json` reports 297 pre-existing diagnostics before and after the pick with `http-server.ts` unchanged at 1, i.e. no new diagnostic. |

## Support and end of life

This line remains eligible for relevant security and correctness updates while
released products still require Midnight Node 1.x on mainnet, preview, or
preprod. A network rollout does not silently archive the branch. Normal
maintenance ends only after the user records an explicit EOL decision and a
migration/EOL window is announced.

Consumers should validate each `0.104.x` update in their own Node-1 deployment
before production rollout.
