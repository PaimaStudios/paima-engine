### `{{name}}` [![Github link](https://github.githubassets.com/favicons/favicon-dark.png)](https://github.com/PaimaStudios/paima-engine/tree/master/packages/evm-contracts/{{__item_context.file.absolutePath}}) {#{{~anchor~}} }

```solidity
import "@paima/evm-contracts/{{__item_context.file.absolutePath}}";
```

{{{natspec.dev}}}

{{#if modifiers}}
#### Modifiers

{{#each modifiers}}
* [`{{name}}({{names params}})`](#xref-{{anchor~}})
{{/each}}

{{/if}}

{{#if has-functions}}
#### Functions

{{#each inherited-functions}}
{{#unless @first}}
##### From {{contract.name}}
{{/unless}}
{{#each functions}}
* [`{{name}}({{names params}})`](#xref-{{anchor~}})
{{/each}}

{{/each}}

{{/if}}

{{#if has-events}}
#### Events

{{#each inheritance}}
{{#unless @first}}
##### From {{name}}
{{/unless}}
{{#each events}}
* [`{{name}}({{names params}})`](#xref-{{anchor~}})
{{/each}}

{{/each}}

{{/if}}

{{#if has-errors}}
#### Errors

{{#each inheritance}}
{{#unless @first}}
##### From {{name}}
{{/unless}}
{{#each errors}}
* [`{{name}}({{names params}})`](#xref-{{anchor~}})
{{/each}}

{{/each}}

{{/if}}

{{#each modifiers}}
<FunctionBlock
    title="{{name}}"
    params="({{typed-params params}})"
    visibility="modifier"
    anchor="xref-{{anchor}}"
>
    {{{escapeChars natspec.dev}}}
</FunctionBlock>

{{/each}}

{{#each functions}}
<FunctionBlock
    title="{{name}}"
    params="({{typed-params params}}){{#if returns2}} → {{typed-params returns2}}{{/if}}"
    visibility="{{visibility}}"
    anchor="xref-{{anchor}}"
>
    {{{escapeChars natspec.dev}}}
</FunctionBlock>

{{/each}}

{{#each events}}
<FunctionBlock
    title="{{name}}"
    params="({{typed-params params}})"
    visibility="event"
    anchor="xref-{{anchor}}"
>
    {{{escapeChars natspec.dev}}}
</FunctionBlock>

{{/each}}

{{#each errors}}
<FunctionBlock
    title="{{name}}"
    params="({{typed-params params}})"
    visibility="error"
    anchor="xref-{{anchor}}"
>
    {{{escapeChars natspec.dev}}}
</FunctionBlock>

{{/each}}