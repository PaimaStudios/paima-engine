
{
  // need this just because a lot of tools depend on the existence of a tsconfig.json
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true,
    "composite": true
  },
  "include": [
    "src/**/*",
    "src/**/*.json"
  ],
  "references": [
    // { "path": "../../paima-sdk/paima-utils/tsconfig.build.json" },
  ]
}