type VersionString = `${number}.${number}.${number}`;

const VERSION_MAJOR: number = 0;
const VERSION_MINOR: number = 0;
const VERSION_PATCH: number = 1;

export const VERSION_STRING: VersionString = `${VERSION_MAJOR}.${VERSION_MINOR}.${VERSION_PATCH}`;
